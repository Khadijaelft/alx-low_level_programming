#include <stdio.h>
/**
 * main - Entry point
 * Description: prints digit numbers
 * Return: always 0
*/

int main(void)
{
	int X;

	for (X = '0' ; X <= '9' ; X++)
	{
	putchar(X);
	}
	putchar('\n');

	return (0);
}
